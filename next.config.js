/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove experimental appDir since it's stable in Next.js 14
  // experimental: {
  //   appDir: true,
  // },
}

module.exports = nextConfig
